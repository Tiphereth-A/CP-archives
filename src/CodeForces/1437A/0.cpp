#include <bits/stdc++.h>
using namespace std;
int main() {
  int kase;
  cin >> kase;
  while (kase--) {
    int n, m;
    cin >> n >> m;
    cout << (n > m / 2 ? "YES" : "NO") << endl;
  __end_kase:;
  }
  return 0;
}
